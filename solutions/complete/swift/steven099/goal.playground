extension String {
    subscript(s: String) -> String {
        return self + s
    }
    subscript() -> String {
        return self + "o"
    }
}

let g = "g"
g[]["al"]
g[][]["al"]
g[][][]["al"]
g[][][][][][][][][]["al"]

